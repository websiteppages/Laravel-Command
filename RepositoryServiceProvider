<?php

namespace App\Providers;

use App\Repositories\BindingMap;
use Illuminate\Support\ServiceProvider;

class RepositoryServiceProvider extends ServiceProvider
{
    /**
     * Register all repository bindings from BindingMap.
     */
    public function register(): void
    {
        foreach (BindingMap::bindings() as $interface => $implementation) {
            // ✅ singleton — Repository எல்லாம் stateless,
            //               ஒரே instance போதும்
            $this->app->singleton($interface, $implementation);
        }
    }
}
